// Chakra's theme colors
const theme = {
  colors: {
    transparent: "transparent",
    blackAlpha: {
      400: "rgba(0, 0, 0, 0.24)",
    },
    black: "#000",
    white: "#fff",
    gray: {
      50: "#f7fafc",
      900: "#171923",
    },
    // Add more colors if needed
  },
};

export default theme;
